<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.zerock.mapper.UserMapper">
    <select id="getList" resultType="org.zerock.domain.UserVO">
    <![CDATA[select * from tbl_user ]]>
    </select>

    <insert id="insert">
        INSERT INTO TBL_USER (USERID, USERPW, EMAIL, PHONENUMBER, AUTHORITY)
        VALUES (#{userId}, #{userPw}, #{email}, #{phonenumber}, #{authority})
    </insert>

    <select id="read" resultType="org.zerock.domain.UserVO">
        select * from tbl_user where USERID = #{userId}
    </select>

    <delete id="delete">
        delete from tbl_user where userid = #{userId}
    </delete>

    <update id="update">
        update tbl_user
        set userid = #{userId},
            userpw = #{userPw},
            email = #{email},
            phonenumber = #{phoneNumber},
            authority = #{authority}
        where userid = #{userId}
    </update>

</mapper>